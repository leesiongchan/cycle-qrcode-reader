{
  "name": "cycle-qrcode-reader",
  "version": "1.0.1",
  "description": "A Cycle.js Driver for QR Code scanning.",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "prepublish": "typings install && npm run build"
  },
  "author": "Lee Siong Chan (ahlee2326@me.com)",
  "license": "Apache-2.0",
  "peerDependencies": {
    "xstream": "^5.3.6"
  },
  "devDependencies": {
    "@cycle/base": "^4.0.2",
    "typescript": "^1.8.10",
    "typings": "^1.3.2",
    "xstream": "^5.3.6"
  },
  "dependencies": {
    "qrcode-reader": "^0.2.1",
    "webrtc-adapter": "^2.0.2"
  }
}
